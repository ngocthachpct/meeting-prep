# URL of the agent-service (FastAPI + ADK)
AGENT_SERVICE_URL=http://localhost:8000

# API key used by the web frontend to authenticate against this gateway
API_KEY=<your-api-key>

# File path for the action item tracker (persisted across restarts)
TRACKER_FILE_PATH=./data/tracker.json
